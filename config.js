/* ═══════════════════════════════════════════════════════════════════
   CRYSTALLIUM — конфигурация продакшена
   Это ЕДИНСТВЕННЫЙ файл, который нужно редактировать при настройке
   монетизации. Ничего в игре руками менять не нужно.
   После правки — просто перезалейте этот файл на сервер (index.html
   и остальные файлы трогать не надо).
   ═══════════════════════════════════════════════════════════════════ */
window.APP_CONFIG = {

  /* ── AdsGram ──────────────────────────────────────────────────────
     Кабинет: https://adsgram.ai → Sign in through Telegram →
     Add app → создать Ad unit (тип Reward) → скопировать blockId.
     Формат: "int-12345". Пока пусто — показывается демо-ролик
     вместо настоящей рекламы (для проверки, что всё остальное работает). */
  ADSGRAM_BLOCK_ID: "",

  /* Debug-режим AdsGram (тестовые показы, деньги не начисляются).
     На проде обязательно false! */
  ADSGRAM_DEBUG: false,

  /* ── TonConnect ───────────────────────────────────────────────────
     Публичный https-URL файла tonconnect-manifest.json (лежит рядом,
     в этой же папке). Обязательно https и обязательно с вашим
     реальным доменом — без этого кошельки откажутся подключаться. */
  TON_MANIFEST: "https://ВАШ-ДОМЕН/tonconnect-manifest.json",

  /* Включить показ варианта оплаты в USDT (jetton-перевод).
     ВНИМАНИЕ: сейчас это только информационная кнопка — реальная
     проверка jetton-платежей на сервере НЕ реализована (только TON).
     Включайте, только если сами доведёте server.js (см. README,
     раздел "USDT / jetton"). По умолчанию выключено, чтобы не
     показывать игрокам способ оплаты, который не проверяется. */
  ENABLE_USDT: false,

  /* Цена «убрать рекламу» в TON. 1 TON = 1 000 000 000 (нанотон). */
  TON_PRICE_NANO: 1500000000,
  TON_PRICE_LABEL: "1.5 TON",

  /* ── Telegram Stars ──────────────────────────────────────────────
     Slug инвойса из bot.createInvoiceLink (Bot API) либо полная
     ссылка вида https://t.me/$xxxxxxxx. Пока пусто — кнопка оплаты
     Stars скрыта. */
  STARS_INVOICE: "",
  STARS_PRICE: 100,

  /* ── Сервер проверки платежей ────────────────────────────────────
     Базовый URL вашего backend (см. server.js в этом же архиве),
     например "https://api.ваш-домен". Пока пусто — работает только
     демо-активация «без рекламы» (см. REQUIRE_SERVER_PAYMENT ниже). */
  PAY_API: "",

  /* Запретить демо-активацию «без рекламы» без PAY_API.
     На проде, после того как подняли сервер — поставьте true,
     чтобы никто не мог включить «без рекламы» простым нажатием
     кнопки без реальной оплаты. */
  REQUIRE_SERVER_PAYMENT: false,

  /* ── Аналитика ────────────────────────────────────────────────────
     URL, куда клиент шлёт события (см. server.js, эндпоинт
     POST /api/events). Пока пусто — события копятся только локально
     в localStorage игрока и никуда не улетают. */
  ANALYTICS_ENDPOINT: "https://ВАШ-ДОМЕН/api/events"

};
